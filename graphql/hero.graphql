type Query {
  hero(id: ID): Hero!
}

type Hero {
  name: String!
  height: String!
  mass: String!
  hair_color: String!
  skin_color: String!
  eye_color: String!
  birth_year: String!
  gender: String!
  created: DateTime!
  edited: DateTime!
}